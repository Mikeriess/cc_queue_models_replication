version: "2.4"

services:
  simulation:
    build: .
    cpus: 24
    volumes:
      - ./output:/app/output
    command: >
      python run_experiments.py
      --workers 24
      --replications 100
      --days 365
      --output /app/output/results.csv
